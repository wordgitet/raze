# tests

Test suites for parser, decoder, and end-to-end extraction.
`make test` runs parser unit checks (`vint`, signature scan, block CRC) and integration extraction/list scenarios.

Run:

```sh
make test
```
